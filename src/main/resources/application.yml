spring:
  application:
    name: etl-off
  main:
    lazy-initialization: true
  threads:
    virtual:
      enabled: true
  datasource:
    url: jdbc:h2:mem:etloff;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        jdbc:
          batch_size: 500
        order_inserts: true
  batch:
    job:
      enabled: false
    jdbc:
      initialize-schema: always

etl:
  csv-path: classpath:open-food-facts.csv
  chunk-size: 500
  grid-size: 8

server:
  port: 8080

logging:
  level:
    fr.sdv.etloff: INFO
    org.springframework.batch: WARN
